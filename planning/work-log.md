---
title: יומן עבודה
tags:
  - work-log
---

# יומן עבודה

שכבת זיכרון בין שיחות. שורה קצרה לכל משימה מהותית: תאריך + סוג-משימה.

| תאריך | סוג-משימה | תיאור קצר |
|-------|-----------|-----------|
| 2026-06-02 | תחזוקת-מבנה | שינוי שמות תיקיות (`תכנון/`→`planning/`, `הגדרות/`→`Settings/`), ארכוב פתקי התכנון הישנים תחת `planning/_ארכיון/`, הסרת הוק-הפרויקט הישן ב-`.claude/settings.json`. התחלת תכנון מחדש. |
| 2026-06-02 | תכנון | brainstorming מלא של הכלי (קלט=טמפלייט, מנוע גנרי + `template_config`, מנגנוני זיהוי, `local_key`, backfill, גיליונות נגזרים, wizard מודרך ב-Streamlit). מסמך תכנון מאושר נכתב ל-[[תוכנית פיתוח]]. |
| 2026-06-02 | אימות-תשתית | אומת ש-`credentials.json` עובד ושיתוף גיליונות עם ה-service account תקין (הבחנת Editor/Viewer) באמצעות `check_access.py` (כלי-עזר חד-פעמי בשורש הפרויקט; לא חלק מהכלי). |
| 2026-06-02 | מימוש | התחלת קוד: שלד `config/`+`modules/`, מודול `sheets_io` + מסך חיבור (`main.py`, Streamlit). נורות 🟢/🟡/🔴 (Editor/Viewer/עודף-הרשאה) אומתו מול גיליונות אמיתיים. |
| 2026-06-02 | מימוש | מודול `query_builder` (שלב 1): ניקוי קלט רב-שורתי + בניית שאילתת `FieldDefinition`. נוצר `config/template_config.py` עם `DEFAULT_OBJECTS` (מחנה פסח). מסך "שלב 1" ב-`main.py` + ניווט מינימלי (רדיו בצד). הניווט/הנעילות המלאים — סבב נפרד. (commit `969d4cb`) |
| 2026-06-02 | תיקון-UI | שלב 1: בלוק הקוד הוצא מ-RTL ל-LTR (ה-SQL הוצג הפוך), והשאילתה פוצלה לשורות (SELECT/FROM/WHERE). אובחן שכפתור ההעתקה תקין, אך `navigator.clipboard` עובד רק ב-secure context — לפתוח דרך `localhost` ולא כתובת רשת. (commit `0fe6914`) |
| 2026-06-02 | החלטה | סוכם git workflow: commit לפי אבני-דרך + הצעה יזומה ואישור (לא לפי זמן, לא שקט). תועד ב-CLAUDE.md. הצעת עטיפת "מימוש" ל-slash command — נדחתה לעת עתה (הדפוס עוד מתגבש). |
| 2026-06-03 | מימוש | מודול `field_dictionary` (שלב 2): פירוק תוצאת ה-SOQL למילון שדות לפי שם-כותרת (חסין לעמודות-התג של Inspector) → `ParseResult` (objects + warnings). נוספה `sheets_io.read_values`. אומת מול הגיליון האמיתי (219 שורות, 4 אובייקטים) + נתיבי-קצה (אזהרה/כותרת-חסרה/ריק/שורה-קצרה). בלי מסך UI עדיין. (commit `0d9b030`) |
| 2026-06-03 | תכנון | נקרא טמפלייט אמיתי ("טיק-צ'ק", לשונית "טמפלייט טעינה"). מבנה: 4 שורות כותרת (0=בלוקים, 1=הסבר, 2=שם עברי, 3=API סיילספורס), דאטה משורה 4. 3 בלוקים: איש-קשר ראשי/אירוע/איש-קשר נוסף. תובנה: שורה 3 כבר ממופה ע"י בונה-הטמפלייט → תפקיד ה-mapper הוא **ולידציה + תיקון**, אך חייב לתמוך גם במיפוי חסר (dropdown בלבד ל-v1, בלי ניחוש אוטומטי עברית→API). |
| 2026-06-03 | מימוש | מודול `mapper` פרוסה 1: `extract_columns` — חילוץ עמודות מ-4 שורות-הכותרת (forward-fill לכותרות בלוק) → `TemplateColumn`. מבנה שורות-הכותרת ב-`template_config`. אומת מול הטמפלייט האמיתי (35 עמודות, נודדות מזוהות). הבא: פרוסה 2 (מיפוי אובייקטים) ופרוסה 3 (ולידציה). (commit `cf86aa7`) |
| 2026-06-03 | מימוש | מודול `mapper` פרוסה 2: `assign_objects` — בלוק→אובייקט + חריגי נודדות (לפי תווית) + סימון ignored למפרידים/תיאור. מפות `BLOCK_TO_OBJECT`/`WANDERING_OVERRIDES` ב-`template_config`. אומת מול הטמפלייט: 2 בלוקי Contact, נודדות ל-CampaignMember/Relationship, אפס לא-ממופות. הבהרת משתמש: "מחיר כרטיס ליחיד"→CampaignMember (רק כש"משתתף באירוע"=TRUE — היגיון שיטופל בגזירת CampaignMember). הבא: פרוסה 3 (ולידציה מול המילון). (commit `18453f1`) |
| 2026-06-03 | מימוש | מודול `mapper` פרוסה 3: `validate_columns` — סיווג כל עמודה (valid/invalid/missing/ignore/no_dict) מול המילון, עם `normalize_api` (חילוץ "Name (Campaign Name)"→"Name") ו-`candidates_for` ל-dropdown. אומת: שדות חופפים ✅, החילוץ עבד. **תובנה:** גיליון ה-SOQL לדוגמה הוא מ-org אחר/חלקי מהטמפלייט (חסרים FirstName/שדות מותאמים) → הרבה 🔴 צפויים; לריצה אמיתית צריך ייצוא FieldDefinition מאותו org של הלקוח. פתוח: טיפול בעמודות-בקרה ("משתתף באירוע"). (commit `92f2846`) |
| 2026-06-03 | מימוש | `mapper` עמודות-בקרה: סטטוס `STATUS_CONTROL` + פרמטר `control_columns` ל-`validate_columns`, ו-`CONTROL_COLUMNS` ב-`template_config` ("משתתף באירוע"). דגלי-בקרה כבר לא מסומנים 🟡 חסר אלא 🎚️ בקרה (נצרכים ע"י ה-splitter בהמשך). אומת. (commit `f96dbec`) |
| 2026-06-03 | החלטה | סוכם לעצור לסבב-UI של המיפוי (שלבים 2–3) לפני המשך המנועים — המיפוי הוא החוזה שכל המנועים הבאים צורכים, ולכן נקודת אינטגרציה לקיבועו. הוחלט לפרוס: A=תצוגת ולידציה (קריאה בלבד), B=עריכה+נעילה. |
| 2026-06-03 | מימוש-UI | מסך מיפוי פרוסה A (קריאה בלבד) ב-`main.py`: קורא טמפלייט+SOQL מ-session של שלב 0, מריץ חילוץ→מיפוי→ולידציה, ומציג סיכום-נורות, רשימת "דורש תשומת לב", וטבלת כל העמודות. נוסף `TEMPLATE_TAB` ל-`template_config`. אומת ב-preview מקצה-לקצה על הגיליונות האמיתיים (ללא שגיאות). הבא: פרוסה B (עריכה+נעילה). (commit `ee7951f`) |
| 2026-06-03 | החלטה | חידוד `identity`: 3 המנגנונים נבחרים ע"י המשתמש במסך (multi-select של שדות-API מהעמודות), כמו אנלוגיית ה-SOQL — המנוע גנרי ולא מקבע שום צירוף. מנגנון 1 = ת"ז כברירת-מחדל אך ניתן לשינוי; כל מנגנון ניתן לכיבוי; הסדר = עדיפות. סדר בנייה: מנוע גנרי → מסך בחירה. |
| 2026-06-03 | מימוש | `identity` מנוע גנרי: `compute_key(record, mechanisms)` + `normalize` + `KeyResult`. מנגנונים מדורגים (ראשון שכל שדותיו מלאים מנצח), מחזיר מפתח + אינדקס-מנגנון (ל-__נמצא_לפי) או None. אומת סינתטית: עדיפות, צירוף, נירמול רווחים/אותיות, דרישת כל-השדות, לא-נתפס, כפילויות. (הטמפלייט ריק מדאטה → אימות על אמיתי יידחה לכשיהיו רשומות). נירמול מינימלי — ללא הסרת מקפים/ספרות לטלפון (שיפור עתידי אפשרי). הבא: מסך בחירת-מנגנונים. | `KNOWN_STANDARD_FIELDS` + `_address_components` — שדות-תקן תקפים-לשליפה שאינם ב-FieldDefinition (Contact: Salutation/FirstName/LastName + רכיבי Mailing/Other address). הושמטו תלויי-הגדרה (StateCode/CountryCode, MiddleName/Suffix). אומת: 4 התרעות-השווא נעלמו → מיפוי נקי (29 ✅, 0 🔴/🟡). |
| 2026-06-03 | מימוש-UI | מסך מיפוי פרוסה B (עריכה+נעילה): נוספה `sheets_io.write_cells` (כתיבה כירורגית, batchUpdate) + `_col_letter`. במסך — dropdown לכל עמודה 🔴/🟡 (מועמדים מהמילון + "אחר/הקלד ידנית"), תצוגה-מקדימה, וכפתור "נעל ושמור" שכותב לשורת ה-API בטמפלייט. הוחלט (אחרי לחיצת המשתמש) ב' = כתיבה-חזרה לטמפלייט (מקור-אמת יחיד); כתיבה כירורגית + אישור. אומת: רינדור אזור העריכה, 5 dropdowns, מועמדים תקינים, וכתיבה חיה (באישור המשתמש): סוג הקשר→npe4__Type__c נכתב לטמפלייט והפך 🟡→✅ (25 ✅, 0 🟡; נותרו 4 🔴 = התרעות-שווא של שדות מורכבים). |
| 2026-06-03 | מימוש-UI | זיכרון-גיליונות (MRU) פר-תפקיד: מודול `recent_sheets` (JSON מקומי `.recent_sheets.json`, ב-.gitignore), `RECENT_SHEETS_FILE` ב-settings, ובמסך החיבור selectbox של אחרונים (לפי שם, אחרון-ראשון) + "קישור חדש"; חיבור מוצלח נשמר. בנוסף שונה שם שדה החיבור השלישי ל"מיפוי אובייקטים ושדות" (3 מקומות בקוד + מסמך התכנון). אומת מקצה-לקצה (3 ירוקים, נשמר ושרד הפעלה מחדש). |
| 2026-06-03 | תובנה | קבצים "עבור קלוד" חדשים שותפו (טמפלייט `1K9Zt`, מילון `1gtW`, DB `1aau`). ולידציה מול המילון הנכון: 24 ✅, 4 🔴 (רק שם פרטי/משפחה), 1 🟡 (סוג הקשר). **מגבלת ולידציה שהתגלתה:** שדות מורכבים (`FirstName`/`LastName` כרכיבי `Name`, וכן רכיבי `Address`) **לא** מוחזרים ב-FieldDefinition אך תקפים ל-SOQL → הולידציה מסמנת אותם 🔴 false-positive. **לטיפול עתידי:** allowlist של רכיבי-תקן מורכבים. נוצרו 4 שאילתות ייצוא חד-פעמיות למשתמש (Contact/Campaign/CampaignMember/Relationship). |
| 2026-06-03 | החלטה | חידוד סמנטיקת הזיהוי לפני בניית המסך (מאומת מול נתוני-דמה): **(1)** v1 — מנגנונים ל-**Contacts בלבד**; קמפיינים לפי שם בנפרד. **(2)** אותו סט 3-מנגנונים משרת את שלושת השימושים (חיפוש-DB, dedup פנימי, `__נמצא_לפי`). **(3)** "מנצח" = **מצא התאמה**, לא "שדותיו מלאים" → שכבות החיפוש/dedup יקראו ל-`compute_key` **מנגנון-בכל-פעם** וייפלו למנגנון הבא על אי-התאמה; המנוע עצמו תקין. **(4)** התווית `__נמצא_לפי` נרשמת רק אם המנגנון *מצא* (לא רק אם הרשומה מכילה ת"ז). **(5)** dedup פנימי = **קיבוץ עם שרשור** (שתי שורות שחולקות *כל* מנגנון = אותו אדם), לא "ראשון מנצח". **(6)** נירמול ספרות-בלבד לטלפון/ת"ז — פנימי בלבד (לא נוגע בדאטה הנטענת), יחד עם שכבת ההתאמה. |
| 2026-06-03 | מימוש-UI | מסך מנגנוני-זיהוי (`screen_identity` ב-`main.py`): מאגר = שדות Contact תקפים (✅) מהמיפוי הקיים, מיוחדים לפי API; 3 מנגנונים מדורגים (checkbox פעיל + multiselect צירוף-AND), מנגנון 1 דיפולט `ID_Number__c`; תצוגה-מקדימה חיה; שמירה ל-`session_state["mechanisms"]` כ-`list[list[str]]` (קלט `compute_key`). קבועים `IDENTITY_OBJECT`/`DEFAULT_IDENTITY_FIELD` ב-`template_config`. אומת מקצה-לקצה מול הגיליונות האמיתיים: מאגר 10 שדות, ת"ז דיפולט ("יורכב: מנגנון 1: מספר תז"), שמירה תקינה. בלי חסימה קשיחה (אזהרות רכות). |
| 2026-06-03 | מימוש | שלב 4 — מסך ייצוא DB: `build_data_query` ב-`query_builder` (SELECT Id + שדות תקפים); `DB_TAB_NAMES` ב-`template_config`; `screen_db_export` ב-`main.py` — סקשן A (שאילתות לכל אובייקט, expandable, LTR) + סקשן B (ולידציה: גישה, לשוניות, כמות רשומות, עמודת Id). נוסף ל-SCREENS כ-"שלב 4 — ייצוא DB". (commit `7d191a6`) |
| 2026-06-03 | מימוש | שלב 5 פרוסה 2 — `splitter` (Contacts בלבד): `split_object(object_api, rows, columns, data_start_row)` → `SplitRecord` (object_api/block/source_row/values). מקבץ עמודות-תקפות לפי `block`, כל בלוק = רשומה לכל שורת-דאטה; דילוג על רשומה ריקה-לחלוטין; שדות STATUS_VALID בלבד. גנרי (ישמש Campaign בעתיד). אומת סינתטית: 2 בלוקים→2 רשומות, דילוג-ריק, source_row אבסולוטי, סינון INVALID/CampaignMember, וקצה-לקצה דרך `dedup_engine` (החוזה בין המודולים). בלי I/O. |
| 2026-06-03 | מימוש | שלב 5 פרוסה 3 — `output_writer.build_contacts_grid` (טהור, כולל backfill): `META_COLUMNS` (6 עמודות-מטא); `_field_columns` — איחוד clean_api תקפים בסדר יציב; `_consolidate` — ראשון-לא-ריק פר-שדה למיזוג רשומות-חבר; backfill ל-Upsert בלבד (DB ממלא חורים, טמפלייט גובר); דגלי-מטא (`__Status`: ⚠️ ריבוי/⚠️ ללא מפתח). אומת סינתטית: 5 תרחישים + backfill עברו. בלי I/O — כתיבה לגיליון בפרוסה 4. |
| 2026-06-03 | מימוש | שלב 5 פרוסה 1 — מנוע dedup טהור (`dedup_engine`): `deduplicate(records, mechanisms, db_records, digits_only_fields)` → `DedupResult`/`PersonResult`. קיבוץ פנימי בשרשור (union-find טרנזיטיבי, מנגנון-בכל-פעם דרך `compute_key`), הצלבה מול DB במפל מדורג (Upsert/Insert/ambiguous), `local_key` דטרמיניסטי (סדר-הופעה), נירמול ספרות-בלבד פנימי (`DIGITS_ONLY_FIELDS` ב-config). אומת סינתטית — 7 תרחישים עברו (שרשור, אי-מיזוג, מפל-עדיפות, ambiguity, Insert/Upsert, לא-נתפס, ספרות-בלבד). בלי I/O — output_writer בפרוסה נפרדת. |
| 2026-06-04 | תכנון-עתידי | QA מקיף — מוסכם לבצע אחרי פרוסה 8 (לאחר השלמת כל פיצ'רי Contacts: פלט חדש + שובר-שוויון + טאב ידני), לפני מעבר ל-Campaigns. הגישה: לשונית-דמה בגיליון האמיתי עם נתונים סינתטיים — כפילויות פנימיות (שרשור + ישיר), כפילויות מול DB (ת"ז / שם+טלפון / שניהם), ריבוי שנפתר בשובר-שוויון, ריבוי שלא נפתר (→טאב ידני), ללא-נתוני-זיהוי, תאים ריקים/תווים מיוחדים/מקפים, ריצה כפולה (אידמפוטנטיות). |
| 2026-06-04 | מימוש-UI | שלב 5 פרוסה 5 — תשתית אפליקציה: `layout="wide"` (מסך רחב); `_read_cached` (`@st.cache_data` על `read_values`) — מעבר בין מסכים לא קורא שוב מ-Google; `_sidebar_controls` (🔄 רענן מהגיליונות + ♻️ התחל מחדש בכל מסך); ריקון מטמון אחרי כתיבת תיקוני-API וכתיבת גריד-פלט; טקסט אנושי במסך 5 (הוסרו: צינור/dedup/Insert/Upsert/backfill). אומת: מסך רחב, כפתורים נראים, אין איפוס במעבר-מסך. |
| 2026-06-03 | מימוש | שלב 5 פרוסה 4 — I/O + מסך Contacts: ב-`sheets_io` נוספו `rows_to_dicts` (גריד→`list[dict]`, חסין שורות-קצרות, מדלג ריקות), `ensure_tab` (addSheet אידמפוטנטי) ו-`write_grid` (clear+update RAW מ-A1, ריצה-חוזרת דורסת נקי). `OUTPUT_TAB_CONTACTS = "פלט - Contacts"` ב-`template_config` (כתיבה לתוך הטמפלייט — אפשרות א', כבר Editor). מסך `screen_contacts` ב-`main.py`: צינור מלא (mapping→`split_object`→`rows_to_dicts(DB)`→`deduplicate`→`build_contacts_grid`) + סיכום-נורות (Insert/Upsert/⚠️) + תצוגה-מקדימה + כפתור-כתיבה. אומת: `rows_to_dicts` סינתטי, וצינור קצה-לקצה טהור (split+dedup+grid: מיזוג ספרות-בלבד→Upsert עם Id מה-DB + Insert). קצה-לקצה מול גיליונות אמיתיים — בהרצת המשתמש. |
| 2026-06-04 | מימוש | שלב 5 פרוסה 6 — `output_writer.build_contacts_grid` שוכתב: 2 שורות כותרת (עברית מעל API), סדר עמודות חדש (לא-נטענות → Id → שדות), הסרת עמודות-מטא טכניות. `sheets_io` הורחב: `_sheet_id`, `set_tab_rtl`, `color_cells` (🟢/🟡/🔴 לתא "נמצא לפי"). `screen_contacts` מחווט מחדש. אומת סינתטית ומול גיליונות אמיתיים. |
| 2026-06-03 | מימוש-UI | שיפורי UI — מסך מיפוי inline, מיזוג שלב 0+1, מקרא שלם, כותרות RTL (commit `20b3d2a`): (1) `screen_mapping` שוכתב לטבלה ניתנת-לעריכה עם dropdown אובייקט+API פר-שורה (`st.columns`+`st.selectbox`). סדר עמודות מימין: שם עמודה · אובייקט · API · נורית · עמ' — אומת ויזואלית. (2) מקרא נוריות: כל 6 סטטוסים תמיד כולל ספירת-אפס (🔴/⚠️ שחסרו). (3) `screen_soql` מוזג ל-`screen_connection` כ-expander — SCREENS נשאר 3 מסכים. (4) CSS: `[data-testid="stHeading"]/h1-h4` מיושרות לימין. (5) `col_letter` חשוף כציבורי ב-`sheets_io`. (6) `_apply_object_overrides` מוזרק ל-pipeline לפני `validate_columns`. ראה [[תוכנית פיתוח]]. |
| 2026-06-04 | מימוש | שלב 5 פרוסה 7 — **צירוף-מנגנונים מדורג** ב-`dedup_engine._match_db`: במקום לעצור על ריבוי-התאמה הראשון, מוצאים עוגן (מנגנון ראשון שתפס); עוגן עם >1 → חיתוך מדורג עם מנגנוני-ההמשך עד צמצום ל-Id יחיד (הכרעה-בשילוב) או מיצוי → ambiguous. `PersonResult` הורחב ב-`match_ids` (מועמדי-DB ל-ambiguous) ו-`combined_mechs` (לתווית/צבע). מונים שונו לספירה הדדית-בלעדית (ambiguous/unkeyed לא נספרים כ"חדשים"). אומת סינתטית: 6 תרחישים (עוגן-יחיד, צירוף→Upsert, ambiguous אמיתי, unkeyed, Insert, קונפליקט-חיתוך-ריק). **תיקן באג שקט:** קודם רשומה דו-משמעית נכתבה לפלט כ-Insert עיוור (כפילות). |
| 2026-06-04 | מימוש | שלב 5 פרוסה 8א — לשונית "טיפול ידני": `build_contacts_grid` מדלג על ambiguous/unkeyed (גריד-טעינה נקי); `_found_by_cell` קיבל ענף הכרעה-בשילוב ("שילוב 2+3", צבע 🟠) + `_CELL_COLORS["orange"]` ב-`sheets_io`. נוסף `output_writer.build_manual_grid` (שורת-מקור + שורות-מועמד זו-לצד-זו, עמודת "בחר", `source_row` 1-based) ו-`OUTPUT_TAB_MANUAL_CONTACTS` ב-config. אומת סינתטית. |
| 2026-06-04 | מימוש | שלב 5 פרוסה 8ב — כתיבה-חזרה מהבחירה הידנית: `output_writer.parse_manual_choices` (קורא ✓ מעמודת "בחר" → {local_key→Id}), `build_contacts_grid(manual_choices=)` (נבחר → Upsert עם backfill, תווית "נבחר ידנית" 🟠), `build_manual_grid(marked=)` (משמר ✓ בכתיבה-חוזרת). ב-`screen_contacts` — **כפתור יחיד** "בנה וכתוב גיליונות טעינה": קריאה-טרייה של הלשונית הידנית, קליטת בחירות, כתיבת שתי הלשוניות (אידמפוטנטי, ללא footgun). אומת סינתטית מקצה-לקצה (סיבוב→סימון→שימור→Upsert). קצה-לקצה מול גיליונות אמיתיים — בהרצת המשתמש. |
| 2026-06-04 | QA | אימות פרוסות 7–8 מול הגיליונות האמיתיים (דיבוג ע"י סקריפט קריאה-בלבד שמשחזר את צינור שלב 5). שני מקרים דו-משמעיים אמיתיים זוהו נכון (שירה לפי ת"ז, שגיא לפי שם+מייל), בחירה→"נבחר ידנית" עם Id נכון, אידמפוטנטי. **תובנה:** "כפילות" שזוהתה = אותה ת"ז על **שני Salesforce Id שונים** (לא השוואת Id — `_build_db_indices` אוסף `מפתח→set(Id)`, set מאחד Id זהים). תקלת-דרך: נתוני-בדיקה עם Id כפול (שורות שתולות שחלקו Id עם רשומות אמיתיות) שברו את תצוגת-המועמד; בנתוני-אמת Id ייחודי תמיד → לא תוקן בקוד (מודע, באישור המשתמש). |
| 2026-06-04 | מימוש | שלב 5 חלק ד' — **CampaignMember**: `modules/campaign_member_builder.py` חדש (טהור): `CMRecord`, `_is_participating` (TRUE/false/0/לא), `_cm_field_columns` (union STATUS_VALID כל הבלוקים), `derive_campaign_members` (עמודת-בקרה per block, 0/1/2 רשומות לשורה, גזירת Ids ממפות פלט, warning על חסר-Id), `build_campaign_member_grid` (2 כותרות, עמודת-שם אדומה-בהיר). `config`: `CM_OBJECT/CM_CONTACT_ID_FIELD/CM_CAMPAIGN_ID_FIELD/CM_PARTICIPATING_LABEL/OUTPUT_TAB_CM`. `screen_campaign_members` ב-`main.py` (כפתור יחיד, מחבר contact_id_map+campaign_id_map ע"י `contact_id_map_from_grid` גנרי). `tests/test_campaign_member_builder.py` — 14 בדיקות. **72 בדיקות, 0 כשלים**. קצה-לקצה — בהרצת המשתמש. |
| 2026-06-04 | מימוש | שלב 5 חלק ג' — **Relationships**: `modules/relationship_builder.py` חדש (טהור): `RelRecord`, `derive_relationships` (גזירת קשר מצמד ראשי+נוסף, מיון-זוג, סינון-DB, שם לתצוגה), `build_relationship_grid` (2 כותרות, עמודות-תצוגה אדומות-בהירות), `contact_id_map_from_grid` (מבנה 2-header של פלט-Contacts), `db_rel_pairs_from_records` (זוגות ממוינים, שני-כיוונים=זוג-אחד). `config`: `RELATIONSHIP_OBJECT`/שדות/בלוקים/`OUTPUT_TAB_RELATIONSHIPS`. `screen_relationship` ב-`main.py` (כפתור יחיד — המשתמש לוחץ לאחר טעינת Contacts + Ids, _read_cached.clear לפני ריצה, מונים חדשים/קיימים/ממתינים, אזהרות per-row). `tests/test_relationship_builder.py` — 16 בדיקות. **58 בדיקות, 0 כשלים**. קצה-לקצה — בהרצת המשתמש. |
| 2026-06-04 | מימוש | שלב 5 חלק ב' — **Campaigns**: שכבה דקה מעל המנוע הגנרי. `template_config`: `CAMPAIGN_OBJECT`/`CAMPAIGN_NAME_FIELD="Name"`/`CAMPAIGN_MECHANISMS=[["Name"]]`/לשוניות-פלט. `dedup_engine.deduplicate` קיבל `local_key_prefix` (ברירת-מחדל `"C"` → תואם-לאחור; קמפיינים `"K"`). `output_writer.build_campaigns_grid` (זהה ל-Contacts פרט לתא "נמצא לפי": "קיים" 🟢/ריק, בלי מספרי-מנגנון — לשם מנגנון יחיד). `screen_campaigns` ב-`main.py` (dedup לפי שם, בלי מסך-זיהוי, משתמש-מחדש ב-`build_manual_grid`/`parse_manual_choices` הגנריים). `tests/test_campaigns.py` — 9 בדיקות (קיבוץ-לפי-שם, נירמול, Insert/Upsert, backfill, ambiguous/unkeyed→ידני, בחירה-ידנית). **42 בדיקות, 0 כשלים** (Contacts לא נשבר). קצה-לקצה מול הגיליונות — בהרצת המשתמש. |
| 2026-06-04 | QA-רשת-בדיקות | **שכבה 1 (pytest):** 4 קבצי-בדיקה ב-`tests/` — identity/splitter/dedup_engine/output_writer. 33 בדיקות, 0 כשלים. מכסות: שרשור, Upsert/Insert/ambiguous/unkeyed, צירוף-מדורג, נירמול ספרות, backfill, קונסולידציה, טיפול ידני, אידמפוטנטיות, ורגרסיית-פרוסה-7 (דו-משמעי לא נכתב כ-Insert). לא נמצאו באגים. **שכבה 2 (e2e):** `qa_e2e.py` — 21 תרחישים מול גיליונות אמיתיים (נתוני-דמה בלשוניות "QA - *" בלבד), 21 עברו. אושר חזותית ע"י המשתמש. Contacts נעול — מוכן ל-Campaigns. |
| 2026-06-04 | מימוש | שלב 6 — **ולידציה**: שני מודולים טהורים חדשים. `modules/formatter.py` — `normalize_text` + `parse_date` (פורמטים מקובלים→ISO, לא-נפרסר→None). `modules/validator.py` — `Issue` + `validate_mapping` (סטטוסי-עמודה: INVALID→שגיאה, MISSING/NO_DICT→אזהרה), `validate_dates` (עמודות Date לפי DataType מהמילון → תאים שלא-נפרסרו, מיקום מדויק `<אות><שורה>`), `validate_ids` (אורך Id≠18 בייצוא DB), `build_issues_grid` (2 כותרות, שורות-שגיאה אדומות). `config`: `OUTPUT_TAB_ISSUES="בעיות"`. `screen_validation` ב-`main.py` (קורא בלבד, מטריקות שגיאות/אזהרות, כתיבה ללשונית "בעיות"). אין שינוי בקוד הנעול. `tests/test_formatter.py` (14) + `tests/test_validator.py` (16). **102 בדיקות, 0 כשלים**. קצה-לקצה — בהרצת המשתמש. |
| 2026-06-04 | תיקון+UX | תיקון מקור-Id לקשרים (`id_map_from_grid` קורא `__Id` של Inspector במקום עמודת `Id`), ושיפורי מסך טיפול-ידני (תיבות-סימון TRUE/FALSE, ירוק להתאמה, רקע-בלוק מתחלף). **109 בדיקות**. אומת חי ע"י המשתמש. ראה [[תוכנית פיתוח]]. |
| 2026-06-04 | UX | שיפורי חוויית-שימוש (לפי פידבק המשתמש): **(א) פאנל-צד** — שמות כפתורים ברורים ("רענן נתונים"/"רענן ואפס הגדרות"), זה-לצד-זה מעל הקו, תיבת-הערות אישית שנשמרת ל-`.notes.txt` (`modules/notes_store.py`), מספור-שלבים רץ 1–8, הרחבה קלה. **(ב) מסך מיפוי** — עמודות-בקרה מגיעות עם dropdown ריק, "אחר"+"ריק" בראש רשימת ה-API, נוריות גדולות + רקע-שורה אדמדם/צהבהב לכל מה שאינו "התאמה". **(ג) ולידציה מבוזרת** — מסך-הוולידציה הנפרד הוסר; בדיקות-נתונים (תאריכים/אורך-Id) רצות בכל מסך-בנייה (`validator.validate_output_grid`), אוטומטית + כפתור "בדוק שוב", עם סיכום במסך, צבע `error` אדום-עז על התא, והערת-תא (`sheets_io.set_cell_notes`/`reset_data_format` לאידמפוטנטיות). שלבי-טעינה 7–11 נסגרו כלא-נדרשים ל-v1. **115 בדיקות, 0 כשלים**. קצה-לקצה — בהרצת המשתמש. |
| 2026-06-05 | תיקון-סביבה | `notes_store.py` הוסר תלות ב-`config.settings` (נתיב מחושב עצמאית עם `Path(__file__)`). `.bat` הפעלה תוקן: הורג תהליכים על פורט 8501 לפני הפעלה (מניעת ריצת מופעים ישנים). שורש הבעיה: 4 מופעי Streamlit ישנים מ-7:18 ב׳ (עם קוד לפני התיקונים) לא נסגרו בין הרצות. |
| 2026-06-05 | תיקון | `REQUIRED_DB_FIELDS` ב-`template_config` — שדות-lookup נגזרים (`npe4__Contact__c`/`npe4__RelatedContact__c` לקשרים, `ContactId`/`CampaignId` ל-CampaignMember) נוספים אוטומטית לשאילתות ייצוא-DB אף שאינם בטמפלייט. מסך מיפוי: expander "עמודות מוסתרות" שמציג את עמודות ה-IGNORE (spacers) עם אות-עמודה. |
| 2026-06-05 | QA | נוסח מסמך בדיקות QA מלא מקצה-לקצה → [[בדיקות-QA]] (14 שורות-דמה D1–D14 לכל תרחישי הכפילויות, בדיקות לכל 8 המסכים + UX + ולידציה). מסמך-הנחיה לסשן-מבצע נפרד; נתוני-הדמה לא נוצרו עדיין. |
| 2026-06-05 | QA-ביצוע | סבב QA מלא בוצע אוטומטית (`qa_run.py`+`qa_probe.py`, כלי-עזר). הוזרקו D1–D14 מתחת לדאטה האמיתית; ערכי D6/D7/D8/D11/D13 נשלפו אמיתיים מה-DB. **שכבת-נתונים 35/35 ✅** (משחזרת את צינור כל מסך), **שכבת-UI** (Streamlit חי, נהיגה בדפדפן) — מסכים 1–4+UX עברו; מסך 5 החי נתן בדיוק אותן תוצאות (33→30, 6 חדשים/22 לעדכון/1 ריבוי/1 ללא-מפתח). 3 ממצאים תועדו ב-[[תוצאות-QA]]: (1) חסר מנגנון 3=טלפון במסמך לשרשרת D4↔D5; (2) `נמצא לפי` ריק במיזוג-פנימי (תקין — לא Upsert); (3) **מסך מנגנוני-זיהוי מתאפס ויזואלית בניווט** (`main.py:419-431`) — הקונפיג השמור עובד אך עלול לאבד מנגנון 2 בשקט אם שומרים מחדש. נתוני-הדמה נשארו בגיליון. |
| 2026-06-05 | תיקון | ממצא QA-3.3 (#3) תוקן: `screen_identity` ב-`main.py` זורע את מצב הווידג'טים מהקונפיג השמור (`mechanisms`) רק כשהמפתח חסר, במקום ברירות-מחדל קבועות (`value`/`default` הוסרו). חזרה למסך 3 כבר לא מאפסת ולא דורסת בשקט את מנגנון 2. ביקור-ראשון נשאר זהה (מנגנון 1 + ת"ז). **115 בדיקות, 0 כשלים.** אימות-UI חי — בהרצת המשתמש. ראה [[תוצאות-QA]]. |
| 2026-06-05 | UX | מסך קשרים (7) — אינדיקציית בדיקת-כפילויות בראש התוצאות (`st.info` לפני האזהרות/הכתיבה): כמה רשומות-קשר נקראו מה-DB → כמה זוגות ייחודיים (מוכיח איחוד דו-כיווני A↔B, NPSP שומר 2 כיוונים) + כמה דולגו ככפילות. תצוגה בלבד ב-`screen_relationship` — הלוגיקה כבר נכונה ואומתה (QA-7.3). **115 בדיקות, 0 כשלים.** אימות-UI חי — בהרצת המשתמש. |
| 2026-06-05 | QA-עמידות | אומת מקצה-לקצה (סימולציה קריאה-בלבד מול גיליון v2 `1a-yD16`, כולל הדמיית חזרת-Inspector עם `__Id` במיקום-זז) ש**הוספת עמודות לטמפלייט אינה שוברת אף שלב**: Contacts/Campaigns/Relationships/CampaignMember — 28+6 Id נפתרו, 12/12 קשרים ו-23/23 CM קיבלו Id. הסיבה: כל הקריאות לפי שם-כותרת (block/API/`__Id`/`local_key`), לא לפי מיקום-עמודה; `local_key` נחתם לפי סדר-שורות. תנאי: טאב `טמפלייט טעינה`, שמות-בלוקים, ו-API תקין לעמודות החדשות. |
| 2026-06-05 | תיקון-UX | `_validation_summary` ב-`main.py` — הודעת בדיקת-הנתונים פוצלה לפי שלב (`written`/`tab`): בתצוגה-מקדימה "לחץ בנה וכתוב כדי לסמן… (למשל J16)" במקום להבטיח תא אדום שעוד לא קיים; אחרי-כתיבה "סומנו אדום בלשונית {tab} (למשל J16)". 4 אתרי-קריאה אחרי-כתיבה מעבירים `written=True, tab=out_tab`. תצוגה בלבד. **115 בדיקות, 0 כשלים.** |
| 2026-06-08 | UX | שיפורי-פוליש ב-`main.py` (שכבת-תצוגה בלבד, המנועים לא נגעו): (1) dropdown ה-API במסך מיפוי מציג שם-שדה בעברית (Label) אך מזין API; כפילות-תוויות → API בסוגריים. (2) נוריות: `_STATUS_LABEL`→`_LEGEND` מקובץ; INVALID+MISSING אוחדו לנורית 🔴 "התאמה שגויה/חסרה" (ספירה+רקע-שורה מאוחדים); בקרה 🎚️→🏳️; "האובייקט לא נכלל בשאילתא"→"האובייקט לא בשאילתא". (3) הודעת "לא נמצאו בעיות" + "(נבדקו: תאריכים ומזהי Id)". (4) כפתורי-צד: "רענן נתונים"→"רענן את 3 הגיליונות", "רענן ואפס הגדרות"→"רענן ואפס מנגנונים ומיפוי". **115 בדיקות, 0 כשלים.** סרגל-עליון נדחה למשימה נפרדת. |
| 2026-06-08 | מימוש-UI | **סרגל-תהליך עליון + איחוד 8→5 שלבים** ב-`main.py` (שכבת-תצוגה/ניווט בלבד; אין נגיעה ב-`modules/`). brainstorming → תוכנית מאושרת ([[תוכנית פיתוח#סרגל-תהליך עליון]]), מומש ב-5 פרוסות: **A** `_topbar()` (5 שלבים, ניווט בלחיצה דרך `st.session_state["step"]`, כפתור primary לפעיל) + הסרת רדיו-הצד + סרגל-צד צר (280–320px, הפתק 520px). **B** מודל `STEPS` (dict label/screen או subs); שלב 1=`screen_step1` (חיבור+שאילתות+בדיקת-DB); שלבים 4/5 עם תת-ניווט `st.radio` אופקי (לא `st.tabs` — שלא יריצו שני הצינורות). **C** `_set_status(step,text,sub)`/`_status_badge`/`_truncate` — סטטוס lazy לכל שלב (1: n/3 מחוברים · 2: ✅/🏳️/🔴 · 3: ✓+שם-שדה מקוצר · 4: חדשים/קיימים פר-בונה · 5: קשרים/CM). **D** `screen_db_export` פוצל ל-`_db_queries`+`screen_queries`(dropdown שאילתות מילון+ייצוא-DB, תיבת-עריכה + `st.code` להעתקה LTR)+`screen_db_validation`. **E** `_N_MECHANISMS` 3→5. כותרות-מסך מספור-קשיח הוסר. אומת: `AppTest` — 5 שלבים, 0 exceptions, תת-ניווט תקין; **115 בדיקות, 0 כשלים**. אימות חי (RTL/Google) — בהרצת המשתמש. (commit `9c91060`) |
| 2026-06-08 | תזכורת-עתידי | **כשהמשתמש יאמר "מתחילים גרסה 2"** (טרם התחיל): (1) לפתוח branch `v2-dev` מ-`main` הנקי + push; `main` נשאר v1 יציבה. (2) **להכין שני קיצורי-bat בשולחן העבודה** — "הפעלת גרסה 1" (`git checkout main` ואז להריץ) ו"הפעלת גרסה 2" (`git checkout v2-dev` ואז להריץ) — כל אחד מחליף branch לבד, המשתמש (לא-מתכנת) לא מקליד git, לוחץ אייקון ורואה את הגרסה הנכונה. לשמר ניקוי-פורט-8501 מ-`הפעלת האפליקציה.bat`; להוסיף הודעה אם checkout נכשל (שינויים לא-שמורים). (3) סיום v2 → merge ל-`main`. צ'קליסט מלא: `~/.claude/plans/atomic-frolicking-owl.md`. |
| 2026-06-08 | UX | **סבב פוליש שני לסרגל ולמסכים** (לפי פידבק חי; `main.py` בלבד, מאומת חי ב-preview): **S1** CSS — הסתרת `stAppDeployButton` + `stSidebarCollapseButton`, `block-container padding-top` 6rem→2.5rem; הכותרת `st.title` הוסרה מהתוכן ועברה לראש הסרגל-צד (מעל כפתורי-הרענון); `initial_sidebar_state="expanded"` (קריטי — בלעדיו סרגל מכווץ ב-1.58 מוסר מה-DOM והמשתמש ננעל בחוץ). **S2** `_topbar` עוצב מחדש כ"כרטיסי-מצב" צבעוניים (אפשרות B): מספר-בשורת-השם, badge בשורה שנייה (label דו-פסקתי), צביעה דינמית דרך `.st-key-nav_{i}` — ירוק=הושלם/אפור=לא-הושלם/לבן=טרם-בוקר + מסגרת כחולה לפעיל (`_step_state` + דגל `ok` ב-`_set_status`/`status_ok`). **S3** מסך-מנגנונים: תיקון-תזמון ה-badge (סטטוס נקבע *לפני* `st.rerun`), כל שדות-המנגנון ב-badge (לא רק הראשון), פריסת 2 עמודות (1\|2,3\|4,5). **S4** תת-ניווט שלבים 4/5: `st.radio`→כפתורים אנכיים גדולים (58px) עם badge פר-גיליון; `_validation_summary`→`(marks,n_issues)`, חיווי "✅ נבדק/⚠️ N בעיות" נשזר ל-badges (`_val_badge`). **S5** `st.header`→`st.subheader` בכל המסכים + קיצור ההסברים ל-`st.caption` (אזהרות "טען Ids קודם" נשמרו כ-`st.info`). **115 בדיקות, 0 כשלים.** אומת חי: צבעי-סרגל, badge-מנגנונים, חיווי-validation, כותרת-בצד. ראה [[תוכנית פיתוח#סרגל-תהליך עליון]]. |
| 2026-06-10 | תיקון | **באג שאילתת-מילון תקועה** ב-`screen_queries` (`main.py`): עורך-השאילתה זרע-מחדש רק כשהשתנתה **בחירת** ה-dropdown, לא כשהשתנה התוכן שנוצר — לכן עריכת רשימת-האובייקטים השאירה שאילתה ישנה (ובמסלול מחיקה+הקלדה-מחדש אף עורך ריק = "אין שאילתה"). תוקן: זריעה-מחדש לפי `(selected, generated)` — מתעדכן כשהאובייקטים משתנים, ועריכות-ידניות שורדות כל עוד הבחירה/האובייקטים לא השתנו. אובחן ואומת חי ב-preview (Ctrl+Enter הוא ה-commit של `text_area`; שינוי-אובייקטים→שאילתה מתעדכנת, ועריכה-ידנית שורדת rerun לא-קשור). **115 בדיקות, 0 כשלים.** |
